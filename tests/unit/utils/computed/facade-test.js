import facade from 'ember-changeset/utils/computed/facade';
import { module, test } from 'qunit';

module('Unit | Utility | facade');

test("it has no tests", function(assert) {
  assert.ok(false, 'write tests plz');
});
