import { module, test } from 'qunit';
import { check, gen, property } from 'ember-changeset/testcheck';

module('Property | Utility | changeset');

test('imports', function(assert) {
  assert.expect(0);
  console.log(check); // eslint-disable-line
  console.log(gen); // eslint-disable-line
  console.log(property); // eslint-disable-line
});
